{
  "name": "ai-configs",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-sh": "^0.18.0"
  }
}
